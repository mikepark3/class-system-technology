#include <stdio.h>
int powerTrain;

float calculateSteerValue(float track_axis, float yaw)
{
	int steer_lock;
	int c;
	float a;
	float dummy;

	/*power train ; is not float*/
	
	powerTrain = c + steer_lock;

	powerTrain =
			c + steer_lock;

	powerTrain = c + 1;

	if(c++)
	{
	}
	else if(c)
	{
	}

	a = track_axis;
	yaw--;
	steer_lock = track_axis * yaw;

	/*dummy ; is track_axis*/
	dummy = track_axis;
	return dummy;
}

int main()
{
	float speed;
	float target_speed;
	float pitch;
	int b;

	return 0;
}
